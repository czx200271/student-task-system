# Server port
PORT=4000

# MongoDB connection string (used in Week 3)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/student-task-db

# JWT secret (used in Week 4)
JWT_SECRET=your-super-secret-key-change-this

# Frontend origin (for CORS)
CLIENT_ORIGIN=http://localhost:5173
